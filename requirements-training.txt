# Governed Kaggle GPU training environment (Python 3.11).
# Install the Kaggle-provided CUDA-compatible torch build unless a replacement
# is required; do not install bitsandbytes for this LoRA pipeline.
torch==2.6.0
transformers==4.52.4
datasets==3.6.0
peft==0.15.2
accelerate==1.7.0
safetensors==0.5.3
huggingface_hub==0.32.4
PyYAML==6.0.2
matplotlib==3.10.3
sentencepiece==0.2.0
protobuf==5.29.4
